import { expect } from 'chai'
import { List, Map, fromJS } from 'immutable'
import { removeFromCart } from '../src/reductions/removeFromCart'

describe('REMOVE_FROM_CART', () => {
  let initialState = sourceState
  const activity = {
    type: 'REMOVE_FROM_CART',
    item: 'glasses'
  }
  describe('')
})
